# Personal Tools

Drop standalone one-page utilities here. Each subfolder should contain its own `index.html`. Access them at `/tools/{folder-name}`.

Example structure:
```
tools/
  golf-gps/
    index.html
  cs-json-helper/
    index.html
```

Query parameters after the URL are passed directly to the embedded page when it loads inside the main site.
